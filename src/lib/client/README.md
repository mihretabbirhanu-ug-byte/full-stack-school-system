# Client-safe modules

Put helpers here that can be imported from `"use client"` components:

- formatting helpers
- UI-only constants
- browser-only utilities

Do not put secrets, DB access, or password hashing here.

